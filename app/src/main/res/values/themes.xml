<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaleV1_0" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Disable dark mode -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- General Colors -->
        <item name="colorPrimary">@color/dark_purple</item> <!-- Button background -->
        <item name="colorPrimaryVariant">@color/darker_purple</item> <!-- Variant for primary -->
        <item name="colorOnPrimary">@color/white</item> <!-- Text on buttons -->
        <item name="colorSecondary">@color/blue</item> <!-- Secondary colors -->
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnBackground">@color/black</item>

        <!-- Text Colors -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/gray</item>

        <!-- Buttons -->
        <item name="colorButtonNormal">@color/dark_purple</item>
        <item name="buttonStyle">@style/MyButtonStyle</item>
    </style>

    <style name="Theme.TaleV1_0" parent="Base.Theme.TaleV1_0" />

    <!-- Custom Button Style -->
    <style name="MyButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/dark_purple</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
